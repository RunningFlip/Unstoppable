/// <summary>
/// Types for states that can be controlled from the statecomponent.
/// </summary>
public enum StateType
{
    Movement = 0,
    Attacking = 1,
    Interacting = 2,
    Damageable = 3,

    Everything = 4,
}
